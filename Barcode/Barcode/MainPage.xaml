<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Mobile.Forms;assembly=ZXing.Net.Mobile.Forms"
             x:Class="BarcodeScanner.MainPage">

    <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
        <zxing:ZXingScannerView x:Name="scanne" WidthRequest="400" HeightRequest="300" HorizontalOptions="Center" VerticalOptions="Center"/>
        <Button Text="Сканировать" Clicked="ScanButton_Clicked" VerticalOptions="End" HorizontalOptions="Center"/>
        <Button Text="Подключиться к серверу" Clicked="ConnectToServer_Clicked" VerticalOptions="End" HorizontalOptions="Center"/>

        <ListView x:Name="scannedListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Item1}" />
                            <Label Text="{Binding Item2}" />
                            <Button Text="Удалить" Clicked="DeleteButton_Clicked" CommandParameter="{Binding Item2}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
